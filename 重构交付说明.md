# Coffee Order 数据库与业务架构重构 — 交付说明

> 本次重构已完成后端、前端、数据库的适配，并已实际启动验证。以下内容覆盖：数据库设计、测试数据、后端接口、前端适配、配置、curl 全流程测试。

---

## 一、数据库（`coffee-server/sql/init.sql`）

执行后**删除旧库并重建** `coffee_order`，字符集 `utf8mb4`、排序规则 `utf8mb4_unicode_ci`。

新增/变更的关键表与字段：

| 表 | 关键字段说明 |
|----|--------------|
| `sys_user` | 新增 `store_id BIGINT NULL`（空 = 平台管理员/用户/骑手）；`role VARCHAR(20)`：`ADMIN`/`STORE_MANAGER`/`RIDER`/`USER` |
| `store` | `latitude`/`longitude`（替代 lat/lng）、`image`（替代 cover）、`open_time`/`close_time`/`status` |
| `product` | `image`、`price`（基础售价）、`is_recommend`（是否推荐）、`status`（上下架） |
| `product_spec` | `spec_name`（温度/糖度/杯型）、`spec_value`（热/冰/正常糖/少糖/无糖/中杯/大杯）、`price_adjust`（价格调整） |
| `orders` | `pickup_type`（0 自取 / 1 外卖）、`address_id` + `address_snapshot`、`total_price`；状态 `0待支付 1制作中 2待取餐 3已完成 4已取消` |
| `order_item` | `spec_info`（规格拼接，如 `冰/少糖/大杯`）、`quantity`、`price` |
| `cart` | `spec_info`、`price`（单价含规格调整） |
| `user_address`/`feedback` | 保留并优化（`latitude`/`longitude`） |

其余保留：`product_category`、`order_status_log`、`rider_location`、`user_behavior`、`evaluation`、`sys_config`、`sys_login_log`、`sys_oper_log`、`sys_menu`、`sys_role_menu`。

### 测试数据

- **3 家门店**，均位于 (22.54, 113.94) 5 公里内，营业中（实测距离 972m / 1252m / 1419m）。
- 每家门店 9 个商品、3 个分类（经典咖啡 / 瑞纳冰 / 轻食），每个商品带温度/糖度/杯型（或份量）规格，共 27 商品、174 条规格。
- **4 个测试账号（密码统一 `123456`，BCrypt）**：

| 用户名 | 角色 | store_id | 用途 |
|--------|------|----------|------|
| `admin` | `ADMIN` | 空 | 平台管理员，可查看/管理所有门店 |
| `store1` | `STORE_MANAGER` | 1 | 1 号门店商家 |
| `rider1` | `RIDER` | 空 | 骑手 |
| `user1` | `USER` | 空 | 小程序用户 |

---

## 二、后端适配要点

- `SysUser` 新增 `storeId`；登录接口返回 `token + role + storeId`。
- 角色统一大写，`SecurityConfig` 与 `@PreAuthorize` 全部更新：
  - `/api/admin/**` → `hasRole('ADMIN')`
  - `/api/rider/**` → `hasRole('RIDER')`
  - 商家端 `/api/store/**` → `@PreAuthorize("hasAnyRole('STORE_MANAGER','ADMIN')")`
- 新增 `StoreMerchantController`（`/api/store/orders`、`/accept`、`/complete`、`/products`、`PUT /products/{id}`），按当前用户 `store_id` 过滤；平台管理员（store_id 空）可查全部门店；跨门店操作返回 403。
- **修复登录 500**：登录日志写库失败不再影响登录；Redis 不可用时降级为不做失败次数限制；同时重建数据库使所有表与实体对齐。
- `RedisConfig` 注册 `JavaTimeModule`，解决门店详情缓存含 `LocalTime` 反序列化报错的问题。

### 新增/变更的后端文件

- 实体：`SysUser`、`Store`、`Product`、`ProductSpec`、`Orders`、`OrderItem`、`Cart`、`RiderLocation`
- 服务：`AuthServiceImpl`、`StoreServiceImpl`、`ProductServiceImpl`、`CartServiceImpl`、`OrderServiceImpl`、`RiderServiceImpl`、`DashboardServiceImpl`、`RecommendServiceImpl`、`OrderCancelTask`
- 控制器：`StoreMerchantController`（新增）、`OrderController`、`StoreController`、`MerchantController`、`RiderController`、`SecurityConfig`、全部 admin 控制器
- DTO：`LoginResp`（不变）、`UserInfoResp`（+storeId）、`StoreNearbyResp`/`StoreDetailResp`/`ProductListResp`/`SpecResp`/`CartItemResp`/`OrderResp`/`OrderItemResp`（新字段）、`AddCartReq`（specIds）、`CreateOrderReq`（pickupType/addressId）
- 配置：`application-dev.yml`（修复 YAML 结构）、`RedisConfig`

---

## 三、前端适配

### 微信小程序（`coffee-mp`）

- 登录角色分流：`pages/login/login.js` — 仅 `ROLE_USER` 可进入小程序首页；`ADMIN`/`STORE_MANAGER`/`RIDER` 提示“无权使用”并清除登录态。
- 点单页：`pages/store/store.js`、`spec-selector` 组件重写为多维度规格（温度/糖度/杯型，逐维选择，自动算价），`AddCartReq` 传 `specIds`。
- 购物车 / 订单页：字段改为 `image`、`specInfo`、`pickupType`、`totalPrice`；订单状态 0-4。
- 门店卡片/商品卡：`cover`→`image`。

### Web 管理后台（`coffee-admin`，Vue3 + Element Plus）

- 登录按角色分流：`ADMIN` 进入全功能后台；`STORE_MANAGER` 进入**商家工作台**；其他角色拒绝。
- 路由守卫按角色拦截（`router/index.ts`），商家只能访问 `/store/workbench`。
- 新增 `views/store/StoreWorkbench.vue`：展示本门店订单（可按状态筛选），支持 **接单（0→1）** 与 **出餐（1→2）** 按钮；展示本门店商品并支持上/下架。
- 管理员页面全部适配新字段（订单状态 0-4、`pickupType`、`totalPrice`、门店 `latitude/longitude/image`、商品 `image/price/isRecommend`）。
- `api/store.ts` 新增商家端接口。

---

## 四、配置（`coffee-server/src/main/resources/application-dev.yml`）

-

## 五、Web 地址

| 系统 | 地址 | 说明 |
|------|------|------|
| 管理后台（开发模式） | http://localhost:5173 | `admin` / `123456` 登录后为全功能后台 |
| 商家工作台 | http://localhost:5173/#/store/workbench | `store1` / `123456` 登录后自动跳转；可接单/出餐 |
| 后端 API | http://localhost:8080 | Spring Boot |

> 小程序端 `coffee-mp/api/request.js` 的 `BASE_URL` 为 `http://localhost:8080`（局域网内开发机地址），如需真机调试请改成后端所在机器的局域网 IP。

---

## 六、curl 全流程测试

```bash
BASE=http://localhost:8080

# 1. 用户登录（admin/store1/rider1/user1，密码 123456）
curl -s -X POST $BASE/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"user1","password":"123456"}'

# 提取用户/商家 token
USER_TOKEN=$(curl -s -X POST $BASE/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"user1","password":"123456"}' | python -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")
STORE_TOKEN=$(curl -s -X POST $BASE/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"store1","password":"123456"}' | python -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")
ADMIN_TOKEN=$(curl -s -X POST $BASE/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}' | python -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")

# 2. 查看门店（(22.54,113.94) 附近 5km）
curl -s "$BASE/api/store/nearby?lng=113.94&lat=22.54&radius=5000"

# 3. 用户点单：加购商品1（规格：温度=冰、糖度=少糖、杯型=大杯），下单自取
curl -s -X POST $BASE/api/cart -H "Content-Type: application/json" \
  -H "Authorization: Bearer $USER_TOKEN" \
  -d '{"productId":1,"storeId":1,"specIds":[2,4,7],"quantity":2}'
# 下单（规格 ID 可从 /api/store/1 返回的 specs 中获取）
curl -s -X POST $BASE/api/order -H "Content-Type: application/json; charset=UTF-8" \
  -H "Authorization: Bearer $USER_TOKEN" \
  -d '{"storeId":1,"pickupType":0,"remark":"test"}'    # 返回 orderId

# 4. 商家登录后查看本门店订单（状态过滤可选：0/1/2/3/4）
curl -s "$BASE/api/store/orders?status=0" -H "Authorization: Bearer $STORE_TOKEN"

# 5. 接单：状态 0→1
curl -s -X PUT "$BASE/api/store/orders/<orderId>/accept" -H "Authorization: Bearer $STORE_TOKEN"
# 6. 出餐：状态 1→2
curl -s -X PUT "$BASE/api/store/orders/<orderId>/complete" -H "Authorization: Bearer $STORE_TOKEN"

# 7. 商家查看/管理本门店商品
curl -s "$BASE/api/store/products" -H "Authorization: Bearer $STORE_TOKEN"
curl -s -X PUT "$BASE/api/store/products/2" -H "Content-Type: application/json; charset=UTF-8" \
  -H "Authorization: Bearer $STORE_TOKEN" -d '{"status":0}'    # 下架商品2

# 8. 管理员查看全部门店订单与门店
curl -s "$BASE/api/store/orders" -H "Authorization: Bearer $ADMIN_TOKEN"
curl -s "$BASE/api/admin/stores?page=1&size=10" -H "Authorization: Bearer $ADMIN_TOKEN"

# 9. 权限验证：普通用户调用商家接口应返回 403
curl -s -o /dev/null -w "%{http_code}\n" "$BASE/api/store/orders" -H "Authorization: Bearer $USER_TOKEN"
```

> 产品 1（拿铁）在库中的规格 ID：温度 热=1/冰=2，糖度 正常糖=3/少糖=4/无糖=5，杯型 中杯=6/大杯=7。加购 `specIds=[2,4,7]` 即“冰/少糖/大杯”，单价 = 28 + 0 + 0 + 3 = 31 元。

---

## 七、如何一键运行

```bash
# 1) 初始化数据库（会自动删除并重建 coffee_order）
mysql -uroot -pmcfyjcy9hjw --default-character-set=utf8mb4 < coffee-server/sql/init.sql

# 2) 启动后端（需要 JDK 21 / Maven，Redis 与 MySQL 已在本机运行）
cd coffee-server && mvn -o package -DskipTests
java -jar target/coffee-order-1.0.0.jar

# 3) 启动管理后台（开发模式）
cd coffee-admin && npm install && npm run dev     # http://localhost:5173

# 4) 小程序用微信开发者工具导入 coffee-mp，调整 BASE_URL 后运行
```

---

## 八、本次实际验证结果

- 4 个账号登录均返回 200 且 role/storeId 正确。
- 下单 → 商家查本店订单 → 接单(0→1) → 出餐(1→2) → 用户确认(2→3) 全流程通过。
- 外卖单：商家出餐(2) → 骑手可接单列表可见 → 骑手抢单 → 取餐送达(2→3) 全流程通过。
- 普通用户访问 `/api/store/orders` 返回 403；`store1` 操作 2 号门店订单返回 403。
- 管理员可查看全部门店订单与门店列表。
- 管理后台 `vue-tsc + vite build` 通过。

---

## 九、第二轮修复：数据库乱码 / 插入失败 / WXML 报错 / 骑手端

### 1. 数据库乱码与插入失败修复

- 在 `init.sql` 顶部加入 `SET NAMES utf8mb4` 等语句，**不依赖客户端 `--default-character-set` 参数**也能正确导入中文（已验证，存储值为标准 UTF-8，`平台管理员` HEX=`E5B9B3E58FB0E7AEA1E79086E59198`）。
- **旧迁移脚本已移出**：`migration_v2.sql`、`migration_v3.sql`、`fix_v4.sql` 引用的是旧版字段（`lng/lat/cover/price/stock` 等），与新 schema 冲突，会导致 `Unknown column`、`Data too long`、外键失败（1452）以及中文乱码（1366）。现已移动到 `coffee-server/sql/obsolete/`，**请勿再执行它们**。
- 重新执行 `init.sql` 后验证：4 用户 / 3 门店 / 27 商品 / 174 规格 / 9 分类全部插入，中文正常。

### 2. WXML 编译错误修复

- `pages/order-detail/order-detail.wxml` 的 `<wxs>` 中对象字面量使用了未加引号的数字键 `{0:'待支付',...}`，WXS 解析报 `Unexpected token ':'`，并连带触发 `__route__ is not defined` 渲染层错误。已改为 `{ '0':'待支付', ... }`。

### 3. 新增骑手端小程序

- **登录分流**：`pages/login/login.js` — `ROLE_USER` 进入用户首页；`ROLE_RIDER` 进入骑手工作台；其他角色提示无权。
- **骑手页面** `pages/rider/rider`（已注册到 app.json）：
  - 可接配送单：外卖单（`pickup_type=1`、商家已出餐 `status=2`、未分配骑手），支持**抢单**。
  - 我的配送任务：已接/已指派订单，支持**取餐送达**（2→3）。
  - 支持上报位置、下拉刷新、退出登录。
- **后端骑手流程**：
  - 可接单列表：`pickup_type=1, status=2, rider_id IS NULL`。
  - 抢单：仅外卖且待取餐、未被抢时可接。
  - 取餐送达：2→3。
  - 管理员指派骑手不再强行改状态，商家仍可正常出餐。
- 新增 `coffee-mp/api/rider.js`；`RiderOrderResp` 增加 `pickupType/addressSnapshot/totalPrice`。

### 4. 骑手端 curl 验证命令

```bash
BASE=http://localhost:8080
RIDER_TOKEN=$(curl -s -X POST $BASE/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"rider1","password":"123456"}' | python -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")
STORE_TOKEN=$(curl -s -X POST $BASE/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"store1","password":"123456"}' | python -c "import sys,json;print(json.load(sys.stdin)['data']['token'])")

# 商家把一张外卖单接单+出餐（pickupType=1，商家接单0->1，出餐1->2）
# 骑手查看可接单列表（应看到该外卖单）
curl -s "$BASE/api/rider/orders/pending?page=1&size=10" -H "Authorization: Bearer $RIDER_TOKEN"
# 骑手抢单
curl -s -X POST "$BASE/api/rider/orders/<orderId>/accept" -H "Authorization: Bearer $RIDER_TOKEN"
# 骑手我的任务
curl -s "$BASE/api/rider/orders/tasks" -H "Authorization: Bearer $RIDER_TOKEN"
# 骑手取餐送达（2->3）
curl -s -X POST "$BASE/api/rider/orders/<orderId>/pickup" -H "Authorization: Bearer $RIDER_TOKEN"
# 上报位置
curl -s -X POST "$BASE/api/rider/location" -H "Content-Type: application/json" \
  -H "Authorization: Bearer $RIDER_TOKEN" -d '{"orderId":0,"lng":113.9433,"lat":22.5482}'
```
